// function smallestNumber(first, second, third) {
//     let min = Math.min(first, second, third);
//     return min
// }

const smallestNumber = (first, second, third) => Math.min(first, second, third)